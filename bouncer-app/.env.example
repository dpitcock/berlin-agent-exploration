# --- WORKFLOW CONFIGURATION ---
# Options: 'n8n', 'openai'
# If undefined or anything else, the app defaults to MOCK mode (simulated responses)
# This controls both the API logic and the frontend footer display
NEXT_PUBLIC_WORKFLOW=openai

# --- OPENAI MODE CONFIGURATION ---
# Required if NEXT_PUBLIC_WORKFLOW=openai
OPENAI_API_KEY=sk-...

# --- N8N MODE CONFIGURATION ---
# Required if NEXT_PUBLIC_WORKFLOW=n8n
# The webhook URL from your n8n workflow
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/berlin-bouncer

# --- SECURITY CONFIGURATION ---
# Optional: Set a code to lock the app behind a guestlist
# If set, users must enter this code to access the app
# The code is case-insensitive
GUESTLIST_CODE=BERLIN2025
